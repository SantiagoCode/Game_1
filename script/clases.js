// Esta es la clase base de cada elemento en el juego
class Animales
{
  constructor(nombre, url, x, y)
  {
    this.animal = nombre;
    this.url = url;
    this.posicion_x = x;
    this.posicion_y = y;
  }
}
